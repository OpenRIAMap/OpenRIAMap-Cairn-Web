# RIA_REVIEW_STATUS_BOARD_UI_1

Local candidate: connects generic Review status-board contracts to the existing RIA review broker, session identity and existing draggable workbench frame. It preserves the first `保存 / 通过 / 打回` row and inserts only `归档 / 要求修改 / 恢复待审` before the existing `临挂 / 导出 / 删除` row.

Validation: production build, auth route test, review-package contract test and project configuration validation passed.
