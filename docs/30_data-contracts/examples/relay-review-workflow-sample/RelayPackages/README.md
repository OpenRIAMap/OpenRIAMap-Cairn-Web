# RelayPackages sample inbox

This directory demonstrates the review workflow bucket layout introduced by `RELAY_REVIEW_WORKFLOW_CORE_1`.

The sample keeps only `review-status.json` and report placeholders. It references the existing Native RelayPackage sample instead of duplicating package files.
