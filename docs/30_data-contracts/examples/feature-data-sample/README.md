# FeatureData Sample

This directory is a tiny contract fixture for `DATA_FEATURE_REPO_CONTRACT_1`.

It is not runtime data and is not loaded by the current site. It exists only to validate the target `Data_Spilt -> Data_Merge -> Data_Index` contract.

Contents:

```text
Data_Spilt/openriamap-ria/zth/BUD/*.json
Data_Spilt/openriamap-ria/zth/STA/*.json
```

The sample builder writes generated outputs to `.cairnmap-tmp/feature-data-sample/`, not back into this docs directory.
